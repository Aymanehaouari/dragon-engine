# DRAGON free public subdomain

This small Render service exposes the existing DRAGON frontend on a free
`*.onrender.com` address while keeping the Cloudflare Worker as the API upstream.

Default upstream:

`https://dragon-music.aymanehaouari25.workers.dev`

Environment variable:

`DRAGON_UPSTREAM`
